# 12-Agent Status Table

> Last refreshed 2026-07-08. CLAUDE.md's Agent Directory now covers all 12 agents (synced 2026-07-04). **Tally (updated 2026-07-09): 8 of 13 fleet members fully built & verified (incl. Agent 6 — real WhatsApp delivery now proven via the Twilio sandbox); 4 half-built (usable core live, external/institutional half pending — Agents 2, 3, 5, 12); 1 not started — Agent 11 (biometric, deferred to the post-success pension release). Agent 6's only prod gap now is swapping the sandbox number for an approved WhatsApp Business sender.**

| # | Agent | Built? | Reachable how | Notes |
|---|---|---|---|---|
| — | Orchestrator | ✅ | `POST /orchestrator/chat` (REST) · `WSS /ws/session/{id}` (token streaming) · `WSS /ws/voice/{id}` (real-time voice) | All three transports share one turn logic (`graph/chat_turn.py`). Incl. `small_talk` intent (no fake retrieval on greetings), chat-based profile learning, session summaries on disconnect |
| 1 | Eligibility | ✅ | Chat (`eligibility_query` intent) | Vector search over the real scheme DB + `score_eligibility` rerank + grounded Gemini reply |
| 2 | Discovery | 🟡 | `POST /orchestrator/admin/discovery/run` + batch scripts | MyScheme source live and growing (**~1,931 schemes, 41% of the ~4,729 catalog**); PIB/data.gov.in sources built but produce zero data (external block — WAF / missing API key, not a bug) |
| 3 | Application Guidance | 🟡 | Chat (`application_request` intent) + `POST /agents/application/portal-recon` (Citizen JWT) | Guidance path done: curated playbook + grounded LLM fallback, every applyUrl domain-whitelisted. **Read-only live portal recon added 2026-07-09** (`utils/portal_recon.py`): opens a scheme's whitelisted applyUrl and reads the live form fields / required-doc hints / headings to enrich guidance — strictly read-only (GET + parse), whitelist re-checked on every redirect hop, degrades honestly on JS-rendered portals. Full auto-fill/submit deliberately NOT built — needs the citizen's own login/Aadhaar/OTP a bot can't hold and can't be verified without a real filing (same class as NPCI/SPARSH) |
| 4 | Document Verification | ✅ | `POST /agents/document/verify-ppo` (full check) + chat (`document_verify` intent, guidance only) | The document channel is fully built — PPO↔Aadhaar Levenshtein mismatch check, now unit-tested. Text chat is guidance-only *by design* (a text message can't carry a document photo); the browser scanner (`/ocr/scan`) carries the real upload |
| 5 | Grievance / NPCI Monitor | 🟡 | `POST /agents/grievance` + `GET /agents/grievances` + `POST /agents/grievance/{id}/cpgrams-ref` (Citizen JWT) + chat (`grievance` intent) | Recording path done: durable `grievances` collection + CPGRAMS self-filing guidance. **Grievance loop closed 2026-07-09**: citizens can list/track their own grievances (ownership-scoped) and report back the CPGRAMS registration number after self-filing (status `recorded → filed_on_portal`). **pgportal browser automation deliberately NOT built** — unlike Agent 3's safe read-only recon, submitting a grievance can't be verified without filing a REAL complaint to a government officer (spam/abuse of a public system); the honest scope is guide-the-citizen + track-the-loop, not bot-submit. NPCI/SPARSH monitoring still needs institutional access |
| 6 | Nudge (WhatsApp) | ✅ (sandbox) | `GET /agents/nudge/status` + `POST /agents/nudge/optout` (Citizen JWT) + `POST /agents/admin/nudge/trigger` (Admin JWT) | **Built + REAL delivery verified 2026-07-09 via Twilio WhatsApp Sandbox.** Full batch: selects citizens with applications stuck at `saved` >3 days, respects opt-out, dedups per (citizen, scheme, type) within 7 days, composes Hinglish reminder, writes `nudge_log` (TTL 90d). Delivery hop (`utils/whatsapp_sender`, lazy `twilio`) sent a real WhatsApp end-to-end (Twilio accepted, message SID returned) through the sandbox — same code path used for production. `send_test_whatsapp.py` is the one-command delivery check. Remaining for prod: swap the sandbox number for a Twilio WhatsApp Business sender (needs Meta Business approval — Rudra will do after buying a business SIM); the scheduled daily/weekly cron is a Cloud Scheduler wiring (Phase 11) |
| 7 | Financial Planning | ✅ | `GET /agents/financial-plan` and chat (`financial_plan` intent) | Total yearly benefit + value-for-effort ranking. Verified with a real chat message returning a real per-scheme breakdown; also surfaced on the Profile "Pension Seva" tab |
| 8 | Comparison | ✅ | Chat (`comparison` intent) | Top-2 vector search + Gemini comparison |
| 9 | CSC Assist | ✅ | `POST /agents/csc/alternatives` (Operator JWT) + chat (`csc_assist` intent, guidance only) + **frontend dashboard `/csc-dashboard`** | Grounded missing-document alternatives with honest "mandatory, no substitute" answers. Role-gated — citizen tokens get 403, verified with a real CSC_OPERATOR JWT. Now has a real operator UI |
| 10 | Analytics | ✅ | `POST /agents/admin/analytics/run` + `GET /agents/admin/analytics/latest` (Admin JWT) | Real Mongo-aggregation metrics (LLM only for narrative, template fallback), writes permanent `admin_reports`. Its first report exposed two real data bugs — both fixed same day. Weekly cron is Phase 11 work |
| 11 | Biometric Assist | 🟡 | `POST /agents/biometric/challenge` + `POST /agents/biometric/liveness` (Citizen JWT) | **Backend built + verified 2026-07-16.** ACTIVE face-liveness via Google's official **MediaPipe FaceLandmarker** (Apache-2.0 — resolves the "IllumiNet/MobileNetV3" provenance unknown). Server issues a random challenge (blink/turn) + single-use nonce (TTL, anti-replay); frames analysed **server-side in RAM, never persisted**; verdict binds into Agent 12's signed DLC payload. Rejects printed photos (zero motion), no-face, multi-face, unperformed challenge — 6 deterministic tests. **Honest limit:** does NOT defeat a determined video replay (needs a trained PAD model / Aadhaar Face RD). **Still to do:** frontend camera-capture UI + on-device test with a real face |
| 12 | Offline Survival Proof | 🟡 | `POST /agents/dlc/{register-key,verify}` + `GET /agents/dlc/status` + Profile → Pension Seva UI | Core built + verified 2026-07-08: RSA-2048 WebCrypto offline signing (non-extractable key in IndexedDB) → QR + offline queue → auto-sync on reconnect; server verifies signature, rejects replay/tamper/forgery (9/9 e2e + real browser WebCrypto path). Deferred (institutional/platform, not code): Bluetooth/WiFi-Direct P2P, real SPARSH acceptance |

**Legend**: ✅ built + verified + reachable as designed · 🟡 built + verified but with a real gap (see Notes) · ❌ not started

**The 3 not-started agents (6, 11, 12) are each blocked by something outside the code** — Twilio approval, an ML-model provenance decision, and PWA infrastructure respectively — not by "haven't gotten to it."

## What "wired into the chat graph" means

CLAUDE.md's Orchestrator Routing table says every intent should route to its agent through the chat graph's conditional edges (`ai_service/graph/orchestrator.py`). As of 2026-07-09, `_INTENT_TO_NODE` has real edges for `eligibility_query` (Agent 1), `comparison` (Agent 8), `financial_plan` (Agent 7), `document_verify` (Agent 4, guidance-only), `application_request` (Agent 3), `grievance` (Agent 5), `csc_assist` (Agent 9), `status_check` (a real node — see below), and `small_talk` (a dedicated greeting node — no scheme retrieval). The placeholder node is now reached **only** by the injection guard's `blocked` pseudo-intent (reply already set) or an unknown/future intent. This same graph now backs **all three transports** — REST, text WebSocket, and real-time voice — so voice gets the identical 12-agent intelligence as text, not a separate weaker flow.

**`status_check` wired 2026-07-09** (`ai_service/graph/agents/status_check.py`). It was the last intent still hitting the placeholder ("Status check abhi Spring Boot gateway se hoga"). Now it reads the citizen's OWN application rows directly from the shared `applications` collection (Spring Boot's model; `userId == citizen_id == JWT sub`), read-only — creating/mutating an application stays a Spring REST concern — and returns a grounded per-application status summary in Hinglish (submitted / saved / approved / rejected / disbursed → friendly phrases, externalAppId surfaced when present). Grounded-first: the reply is built deterministically from the real rows (works with zero LLM budget, can't hallucinate a status), with an optional LLM warmth pass that falls back to the deterministic text on any error. Verified end-to-end against real Mongo (seeded 3 real applications → correct newest-first summary; zero-applications and empty-citizen edges handled) + 9 pure-function unit tests in `ai_service/tests/test_status_check.py` (suite now 71). One check deferred to a services-up session: a live chat turn exercising the LLM intent classifier's `status_check` classification (the routing itself is deterministically confirmed).
