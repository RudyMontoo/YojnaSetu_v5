# Remaining Work — Detail

> Organized by the original phase sequencing (see the rebuild plan referenced in memory). Each item says plainly what exists vs. what doesn't — nothing here is rounded up to sound more done than it is.

## Immediate gaps in already-"done" phases

These aren't new phases — they're loose ends inside Phases 3–5:

- ~~Agent 4 and Agent 7 aren't reachable from chat~~ — **closed 2026-07-03.** Both now have real graph nodes (`run_financial_plan_agent`, `run_document_verify_guidance`), verified with real chat messages. Agent 4's chat node is guidance-only by design (real verification needs uploaded document images, which a text message can't carry) — that's not a remaining gap, it's the correct behavior for that channel.
- **PIB RSS + data.gov.in sources produce zero data.** Not a bug — genuinely blocked, and PIB's dead-end status was independently re-verified 2026-07-03 (not just assumed from an older note): `www.pib.gov.in` WAF-blocks non-browser traffic (even `robots.txt` 403s), and both the classic `ViewRss.aspx` URL and `archive.pib.gov.in`'s RSS path return plain HTML pages with zero `<rss>`/`<feed>`/`<?xml>` content, not feeds. If revisited, the productive next move is the same technique that found MyScheme's real API — read PIB's frontend JS bundles for `fetch()` calls — not searching for an RSS URL that no longer functions. data.gov.in needs `DATAGOVIN_API_KEY` + resource IDs found by browsing their catalog with a key.
- **MyScheme sync is at 3,231 / ~4,729 schemes** (2,812 from MyScheme + 419 original). **Empty eligibility rules: 185** — 15 of those are the permanent genuinely-non-extractable ones, the other ~170 are fresh quota-fixable debt from the 2026-07-10 offset-2740 batch (287 new schemes inserted, but quota died mid-ingestion so ~168 landed rule-less per the by-design "insert now, backfill later" pattern). Resume the sync with **`--offset 3040`** next session (NOT the 2,812 doc-count — see the offset-drift fix below and the corrected rule in SYNC_TASK_HANDOFF.md); run `run_rules_backfill` on a fresh-quota day to clear the ~170. Progress toward the state-scraper re-evaluation trigger: **68% synced**. Batch history: three batches completed 2026-07-08 — `--offset 1512` (209 inserted / 83 updated / 0 failed), `--offset 1721` (210 inserted / 61 updated / 0 failed), `--offset 1931` (211 inserted / 86 updated / 2 skipped / 0 failed) — the latter two ran with quota already dead so their ~421 new schemes landed with empty `eligibilityRules` per the by-design "insert now, backfill later" pattern (empty-rules peaked at **1,386**); then `--offset 2142` on 2026-07-09 (201 inserted / 9 updated / 90 skipped / 0 failed). See the 2026-07-09 note below for how empty-rules collapsed from 1,386 to 17. Two data-quality repairs: `run_state_rehydration.py` **re-run 2026-07-04 against the new 811 total** (the 246 new schemes hadn't been through it yet) — 105 needed re-checking, 104 processed cleanly + 1 transient connection error (retried, cleared). Final: **105 of 811 still show `state: null`, and that's now believed correct, not a repair backlog** — spot-checked two ("Stand-Up India", "Pradhan Mantri Suraksha Bima Yojana") and both are genuinely national/central schemes with no state in MyScheme's own detail-page data, which is the authoritative source this script reads. Re-run only if a citizen ever reports a scheme that *should* have a state showing as central. `run_rules_backfill.py` healed 189 (576→387) then **hit BOTH providers' daily quota simultaneously** (Gemini free-tier 20 req/day, Groq TPD 100k/day) and stopped itself per its own 8-consecutive-failure safety rule — a daily cap, not a transient error. The MyScheme batch that followed made this worse: empty-rules count rose to **630** (the 246 newly-inserted schemes hit the same exhausted quota during ingestion, per upsert.py's by-design "insert now, backfill rules later" behavior). **Re-tested 2026-07-04 late — quota reset was real but nearly worthless in practice**: a single manual LLM call succeeded, but re-running the backfill hit the ceiling again within ~1-8 calls both times. Root cause is now precisely understood, not just "a daily cap": Gemini's free tier is **20 requests PER DAY, total** (confirmed from the actual 429 error body — `GenerateRequestsPerDayPerProjectPerModel-FreeTier`, `quotaValue: 20`) — any real usage across the day (a few chat turns, one CSC lookup, this backfill's first handful of calls) burns through it in minutes. Groq's 100k-tokens/day budget was already at ~99,986/100,000 used before this session even started today. **Bottom line: this backfill cannot make meaningful progress until either quota resets on a day with near-zero other LLM usage, or a paid tier is added to one provider.** Healed count sits at 629 (net zero progress on the retries today).

**2026-07-05 continuation — fixed a real bug in the script's own diagnostics, and learned Groq's daily quota does NOT reliably reset with the calendar day.** The script previously couldn't tell "the LLM call failed" apart from "the LLM call succeeded but this scheme's eligibility text genuinely has no income/age/category/occupation facts to extract" — both looked like an empty `{}` return. Confirmed this is real: manually called `extract_eligibility_rules` on an actual stuck scheme ("Central Sainik Board (CSB) Welfare Funds", eligibility text "Ex-servicemen and war widows in financial distress") and it returned `{}` cleanly with **zero exception, zero log line** — there's genuinely nothing extractable in text like that. Sampled 5 more stuck schemes and all had similarly narrow, non-extractable eligibility text (unbanked citizens, COVID orphans, existing beneficiaries, minority-block residents, PMAY pilot participants) — meaning an unknown chunk of the "630 broken" figure was never a quota problem at all, just schemes whose eligibility criteria don't map onto this schema's fields.

Fixed: `extract_eligibility_rules()` gained an opt-in `raise_on_error=True` (default `False`, so `upsert.py` and every other caller keep their exact original behavior) that lets `run_rules_backfill.py` distinguish a real exception from a clean empty result. The script now tracks `healed` / `no_rules_found` / `api_failed` separately, and only consecutive real `api_failed` events trigger the early-stop — a run of ordinary narrow-eligibility schemes no longer falsely reports "quota exhausted."

Also learned: on the next calendar day, a raw single manual LLM call succeeded (looked like quota had reset) — but running the fixed script immediately after showed `api_failed=8` with Groq's TPD still at ~99,806/100,000 used. **Groq's "tokens per day" budget is not resetting cleanly at the UTC calendar-day boundary the way "daily quota" implies** — the one manual success was likely the last sliver of a still-mostly-exhausted budget, not evidence of a fresh reset. Re-run `run_rules_backfill.py --limit 700 --pause 6` again later and watch the three-way breakdown in its output — it'll now honestly show how many are real quota-blocked vs. genuinely-unextractable (which no amount of re-running will fix, by design — that's a schema-coverage question, not a backlog).

**Re-tested once more 2026-07-05 (same day, after the MyScheme batch above pushed empty-rules to 867)**: a one-off Groq test call succeeded, so tried a real run (`--limit 900 --pause 4`) — the fixed diagnostics worked exactly as intended: `no_rules_found=3` (real, genuinely-empty extractions, correctly not treated as failures) and `api_failed=8` (Groq's TPD was back at ~99,957/100,000 — the test call itself likely used the last sliver again) → `healed=0`. Confirms the earlier finding: Groq's daily budget on this account is essentially saturated most of the time right now, only clearing for single isolated calls, not enough for a real batch.

**2026-07-07 — real progress finally landed.** Two days later (quota had genuinely accumulated headroom this time, not just an isolated sliver): `run_rules_backfill.py --limit 900 --pause 4` **healed 191 schemes** (867 → 676 empty) before quota ran out again, plus correctly identified 19 more as genuinely non-extractable (not counted as failures, thanks to the diagnostic fix). A second attempt immediately after found the quota already back at ~99,958/100,000 used — confirming once more that a single successful test call doesn't mean batch headroom, only that the last sliver cleared. Then continued the MyScheme sync (see above, `--offset 1051`) which pushed empty-rules back up to **907** (234 new schemes, same by-design pattern). Re-run `run_rules_backfill.py --limit 900 --pause 4` again on a future day with accumulated quota — this is genuinely working, just rate-limited by the free tier's daily budget, not stuck.

**2026-07-07 evening — sync batch ran, backfill correctly skipped.** Batch `--offset 1285` landed clean (227 inserted / 72 updated / 0 failed → 1,512 myscheme, 1,931 total, empty-rules **1,086**). The backfill was NOT attempted: the batch's own log tail showed Groq's TPD at 99,843/100,000 used and Gemini's free tier fully exhausted (429s on both) in the final minutes of the run — direct evidence, fresher than any test call, that today's quota is dead (the morning backfill + this batch's ingestion consumed it). Next session: Job 1 with `--offset 1512`, then the backfill if quota has accumulated.

**2026-07-08 — both jobs ran; batch clean, backfill made modest progress before quota died.** Batch `--offset 1512`: 209 inserted / 83 updated / 8 skipped / 0 failed → 1,721 myscheme, 2,140 total — but its log carried 342 quota-429 lines, i.e. the batch's own extraction calls ate most of the overnight quota reset before the backfill got a turn. Backfill (`--limit 900 --pause 4`) then ran anyway (Groq test call passed): **healed=66, no_rules_found=17, api_failed=8** → stopped on its 8-consecutive-failures rule. Empty-rules 1,167 → **1,101**. Pattern worth noting for future passes: on a fresh-quota day, the sync batch and the backfill compete for the same budget — if healing rules ever becomes the priority over catalogue growth, run the backfill FIRST that day, then the batch. A third same-day batch (`--offset 1721`, quota already dead, fetch-only) then added 210 more schemes → 2,350 total (**50% synced**), empty-rules **1,304**; a fourth (`--offset 1931`) added 211 more → 2,561 total (**54% synced**), empty-rules **1,386**. Next session: Job 1 with `--offset 2142`.

**2026-07-09 — the backfill finally cleared, essentially to zero.** On a genuinely fresh day's quota, empty-rules collapsed from 1,386 to **147** before I resumed this session — the healed docs all carry 2026-07-09 timestamps, so a backfill ran on the day's fresh Groq budget (my own `--offset 2142` sync batch, run early on July-9 quota, healed only ~200 of that drop via its own ingestion-time extraction; the remaining ~1,000 came from a rules-backfill run this day). I then ran `run_myscheme_batch --offset 2142` (201 inserted → 2,762 total, 2,343 myscheme, **58% synced**) and a cleanup `run_rules_backfill --limit 200 --pause 4` on the residual 147: **healed=130, no_rules_found=15, api_failed=2** → **empty-rules now 17**. Of those 17, only ~2 are quota-fixable (the `api_failed` ones); the other 15 are the genuinely-non-extractable narrow-eligibility schemes the diagnostic fix correctly distinguishes. **The rules-backfill job is effectively DONE** — a future run can mop up the last 2 quota-failures, but there is no meaningful backlog left.

**2026-07-10 — found & fixed a real offset-drift flaw in the sync methodology.** The handoff had said to pass the myscheme doc-count as `--offset`. That's wrong: `--offset` is a position in MyScheme's *listing*, but the doc-count only advances by schemes actually **inserted**, and every batch skips/updates some (content-hash-unchanged / already-present), so inserts < the 300 fetch window → the doc-count lags the true listing frontier → re-using it re-fetches already-covered ground. Caught it in the act: `--offset 2343` (= the doc-count) fetched 80 candidates and inserted only **5 new** (72 were re-treads of the previous batch's 2142→2442 window) — and MyScheme also reset the connection mid-run at listing pos 2423 (transient; it does this occasionally, cutting a batch short). Re-ran at the true frontier `--offset 2440` → **177 new inserted** (2,348 → 2,525 myscheme, 2,944 total, **62% synced**), empty-rules still 17 (today's fresh quota extracted rules for nearly all 177 during ingestion). That batch reported exit code 144 — spurious sandbox artifact, the DB writes all landed (verified via Mongo count, per the standing "cross-check durable state, not scratchpad logs" rule; the log file was even auto-deleted on the 144 exit). **Corrected rule (now in SYNC_TASK_HANDOFF.md): advance `--offset` by the fetch window (~300) from the previous batch's *start*, with a small overlap — overlap only costs cheap skips, a gap permanently misses schemes.** The very next batch confirmed the fix: `--offset 2740` fetched 299 candidates and inserted **287 new** (only 1 skip — near-zero re-tread, exactly what a correct frontier offset should look like) → 2,812 myscheme / 3,231 total / **68% synced**. Quota died mid-ingestion (Groq per-minute TPM 12k + Gemini free-tier both hit), so ~168 of those 287 landed rule-less → empty-rules 17 → **185** (fresh backfill debt, not the permanent kind). One malformed scheme (slug `vpby`) failed its detail fetch with an AttributeError and was skipped gracefully (0 failed overall). Next `--offset 3040`, ~5 batches to reach the full ~4,729.
- ~~The 5 state scrapers CLAUDE.md names~~ — **reassessed 2026-07-05, recommendation: defer, don't build yet.** Checked actual current coverage rather than guessing: UP 11, Maharashtra 24, Rajasthan 36, Tamil Nadu 39, West Bengal 19 schemes — looks thin, but 706 of the catalogue's 849 state-attributed schemes already come from MyScheme (only 143 from the original seed data), and MyScheme sync is still only ~26% complete (1,230/~4,729). The low per-state numbers are far more likely an artifact of an incomplete sync than a real MyScheme coverage gap — building 5 bespoke scrapers (each its own robots.txt check, markup, rate limiting, per CLAUDE.md's own multi-step task template) is real, non-trivial work that would very plausibly duplicate what finishing the MyScheme sync already gets for free. **Concrete re-evaluation trigger, not a vague "someday"**: once MyScheme sync is meaningfully further along (say 70%+), re-run this same per-state count query — if a state is *still* thin at that point despite the fuller sync, that's real evidence MyScheme's own coverage has a gap for that state and a dedicated scraper is justified. Don't build these speculatively before that signal exists.
- ~~WS token streaming~~ — **closed 2026-07-04.** `/ws/session/{id}` now streams real generation-time tokens (LangGraph `stream_mode="messages"` tapping the LLM calls inside agent nodes) + one authoritative done frame; ChatPage consumes it with REST fallback. See COMPLETED.md.

## Phase 6 — Agent 3 (Application Guidance) + Agent 5 (Grievance/NPCI Monitor)
**Status: Agent 3's guidance path DONE (2026-07-03); browser automation + Agent 5 not started.**

- Agent 3 guidance path shipped: curated playbook + grounded LLM fallback via the `application_request` chat intent, `domain_whitelist.py` live on every recommended URL. The browser-use automation half remains — unstable markup, CAPTCHA, session forms — and now composes on top of a working fallback instead of being the only path.
- Agent 5 recording path shipped 2026-07-03: `POST /agents/grievance` + `grievance` chat intent persist to the `grievances` collection (status `recorded` — the handoff point for portal automation later) and give CPGRAMS self-filing guidance. Still open: pgportal browser automation, NPCI/SPARSH monitoring (institutional access — mock/swappable design when attempted).
- `domain_whitelist.py` now exists (`ai_service/utils/`) and is enforced on recommended URLs; browser navigation enforcement hooks in when automation lands.
- `status_tracker.py` already exists as a scraper-based fallback path and can stay relevant here.

## Phase 7 — Agent 6 (Nudge) + Agent 9 (CSC) + Agent 10 (Analytics)
**Status: Agents 9 and 10 DONE (2026-07-03, see COMPLETED.md). Only Agent 6 remains.**

- Agent 6 (WhatsApp nudges) is blocked on Twilio WhatsApp Business approval, which has a **1–4+ week lead time** — this should be *applied for now*, independent of when the agent gets built, so the lead time isn't sitting on the critical path later.
- Agent 10's weekly Sunday-11PM-IST cron is a Phase 11 deployment concern; the report itself generates on demand today.

## Phase 8 — Agent 11 (Biometric Assist)
**Status: not started. Novel — biggest single unknown in the whole plan.**

It's genuinely unclear whether pretrained IllumiNet/MobileNetV3 weights exist from a prior "Jeevan-Setu" project, or whether this means training a liveness classifier from scratch. This needs to be resolved before any implementation estimate is meaningful.

## Phase 9 — Agent 12 (Offline Survival Proof)
**Status: not started. Novel.**

~~Needs RSA-2048 signing + QR + Bluetooth/WiFi Direct verification + sync-on-reconnect.~~ **Core BUILT + verified 2026-07-08** (see COMPLETED.md): `POST /agents/dlc/{register-key,verify}` + `GET /status`, RSA-2048 WebCrypto keypair (non-extractable private key in IndexedDB), offline signing → QR + IndexedDB queue → auto-sync on reconnect, replay/tamper/forgery all rejected. Verified with a real RSA keypair (9/9 e2e) AND the actual browser WebCrypto path (Node). Wired into Profile → Pension Seva. **Still deferred (institutional/platform, not code):** Bluetooth/WiFi-Direct peer transfer (not a web API — QR is the substitute) and real SPARSH/pension-dept acceptance (needs institutional integration — the proof is verified+recorded in our own system as the swappable handoff point).

~~**Note**: CLAUDE.md's Agent Directory table only lists 10 agents~~ — **closed 2026-07-04.** CLAUDE.md's Agent Directory, GraphState code block, Orchestrator Routing diagram, and Timeout Table were all re-synced against the real code that day (see COMPLETED.md) — all 12 agents now listed with honest status, `small_talk` intent added to the routing diagram.

## Phase 10 — Frontend Integration + Hardening
**Status: core rebuild DONE (2026-07-03) — full redesign, Supabase removed entirely.**

Shipped: OTP cookie login (no tokens in JS ever), Sathi chat with trending strip + save-to-application chips, Applications page with status timeline + in-context grievance filing, Pension page (PPO/Aadhaar upload check), Profile page (edit + consent + Agent 7 benefit plan + DPDP delete-account), single-origin Vite proxy (cookies flow without CORS ceremony). Backend change that unblocked it: citizen-facing ai_service endpoints now gate on the JWT cookie alone (X-API-Key stays on admin/internal only — browsers can't hold service secrets). **Doc-drift correction (2026-07-08): earlier revisions of this list claimed "PWA manifest" was shipped — it wasn't (no manifest, no service worker, index.html still titled "frontend"). That's now actually built (see below), so the claim is finally true instead of aspirational.**

Still open in Phase 10: real-device testing (now includes the live voice call — verified with synthesized audio through the real protocol, but not yet with a human on a phone mic). ~~CSC operator dashboard~~ — **shipped 2026-07-04**, `/csc-dashboard` (`CscDashboardPage.jsx`), see COMPLETED.md. ~~Voice recorder UI~~ — **superseded and shipped 2026-07-07**: the mic button now starts a live streaming call (Pipecat client, lazy 446KB chunk), not a recorder; see COMPLETED.md. ~~Offline service worker + installable PWA~~ — **shipped 2026-07-08** via `vite-plugin-pwa` (Workbox): real web-app manifest (name/icons 192+512+maskable/theme/standalone/start_url `/home`), precached app shell (24 entries, auto-updating), `NetworkFirst` runtime caching of the read-only `/api/v2/schemes` catalogue so already-seen schemes survive a dropped connection; auth/chat/voice deliberately uncached. SW registered from `main.jsx` (guarded). See COMPLETED.md.

## Phase 11 — Cloud Run Deployment + Pilot Readiness
**Status: deploy scaffolding BUILT 2026-07-09; the actual deploy is blocked only on Rudra's GCP account + Atlas cluster (external, not code).**

Built + verified as far as possible without a GCP project: `ai_service/Dockerfile` (python:3.12-slim + libmagic, binds Cloud Run's `$PORT`), `frontend/Dockerfile` (node build → nginx serve) + `frontend/nginx.conf.template` (reverse-proxies `/api/*`→spring and `/ws,/orchestrator,/agents,/ocr,/voice,/translate`→ai-service, keeping the app **single-origin** in prod so httpOnly cookies work without CORS — the prod mirror of the dev vite proxy; validated with real `nginx -t`), the spring-gateway `Dockerfile` upgraded to multi-stage (builds from source), `deploy/deploy.sh` (deploys all 3 services to asia-south1 via `gcloud run deploy --source`, wires Secret Manager secrets, chains the service URLs), and `deploy/DEPLOY.md` (full runbook incl. the **[YOU]** steps). `.dockerignore` files keep `.env`/venv/node_modules/tests out of images.

**Still needs Rudra (external, can't be done in code):** create the GCP project + enable billing, create the Atlas asia-south1 cluster, run the `gcloud secrets create` commands, then `PROJECT_ID=… deploy/deploy.sh`. Full container image builds (ai_service pulls torch/easyocr; spring runs maven) happen on Cloud Build at deploy time — deferred deliberately (heavy, need the GCP context anyway). Scheduled agents (Agent 6 nudges, Agent 10 analytics) wire to Cloud Scheduler → their admin endpoints. Everything still also runs local-only for dev.

## Other known gaps (not tied to a specific phase number)

- ~~WebSocket endpoints don't exist~~ — **`/ws/session/{id}` (text chat) built and verified 2026-07-03; token streaming added 2026-07-04** (`ai_service/routers/ws_router.py`, shared turn logic in `graph/chat_turn.py`, cookie-JWT auth with 1008 closes on bad tokens, real generation-time token frames). ~~Still open within this: `/ws/voice/{id}`~~ — **closed 2026-07-07**, real-time Pipecat pipeline (`voice_ws_router.py`), see the voice bullet below + COMPLETED.md.
- ~~Voice pipeline not migrated~~ — **FULLY closed 2026-07-07, in two stages the same day.** Stage 2 (the Pipecat half, see COMPLETED.md): `/ws/voice/{session_id}` is now a real-time streaming pipeline — Pipecat 1.5 + Sarvam streaming STT with server-side VAD (`vad_signals=True`, CLAUDE.md's "Saaras V3 exclusive VAD") + a custom `OrchestratorTurnProcessor` splicing `run_chat_turn()` in as the LLM stage + streaming Bulbul v3 TTS, with barge-in. Verified end-to-end with real synthesized Hindi speech over the real protobuf protocol: flawless Devanagari transcription, a real grounded Agent-1 reply (PM Kisan ₹6,000 + Natural Farming ₹15,000/hectare), reply audio streamed back. ChatPage's mic button is now a live call (lazy-loaded Pipecat JS client); the turn-based endpoints below remain as the fallback surface. Stage 1 (earlier same day): **voice now shares the real 12-agent orchestrator.** Investigation before touching anything corrected an earlier hasty note: `ai_service/utils/sarvam.py`'s Sarvam Saaras v3 STT / Bulbul v3 TTS calls were already real and spec-correct (`mode="transcribe"`, `model="bulbul:v3"`, pace-only params) — the actual gap was that voice talked to the OLD pre-rebuild fixed-questionnaire (`agent_router.py`'s `UserProfile` + ChromaDB), completely disconnected from the LangGraph orchestrator text chat uses. A voice user got a fixed fixed state/income/occupation Q&A with no eligibility deep-dive, no financial plan, comparison, grievance, or CSC assist, no small_talk, no persisted history, and no profile learning. Rewrote `ai_service/routers/voice_conversation.py`: `/voice/conversation/answer` now transcribes via Sarvam, runs the transcript through the exact same `run_chat_turn()` REST/WS text chat uses, and speaks the reply back via Sarvam — same brain, audio transport. Added JWT auth (`get_current_citizen_id`, matching every other citizen-scoped endpoint — the old flow had none, harmless when it only touched in-memory state, not once it started writing to `conversation_sessions`/`reasoning_traces`/citizen profile). Session id is now client-generated and **shared with text chat** (`ensureSessionId()` in `ChatPage.jsx`) — speaking and typing in the same tab continue one conversation thread, not two separate ones. Removed the broken, unreachable `/voice/conversation/chat` (referenced an undefined `language` variable — real bug, would have thrown on every call, zero frontend callers). Frontend: `ChatPage.jsx`'s "type while in voice mode" path no longer calls the old `/agent/answer` — text always goes through the same orchestrator path now, matching voice.

  **Verified for real, not just code review**: synthesized a genuine Hindi speech clip via Sarvam TTS itself ("main UP ka kisan hoon, meri income 1.5 lakh hai, mujhe kaunsi yojna milegi"), POSTed it through the real proxy with a real JWT cookie, and got back: correct STT transcript, `X-Intent: eligibility_query`, real personalized schemes (PM Kisan Samman Nidhi, Agriculture Infrastructure Fund — matching what the identical profile returns via text chat), and a real ~1.6MB spoken MP3 reply. Confirmed the turn persisted to `conversation_sessions` with `channel: "voice"` and to `reasoning_traces` — genuinely the same backend pipeline as text, not a parallel one.

  **Still open, deliberately not done in this pass**: no Pipecat, no real-time WebSocket streaming — this is still turn-based (record a clip, upload, wait, get a clip back), not a live low-latency back-and-forth. That was scoped out as a separate, lower-priority effort once the bigger functional gap (voice = weaker experience than text) was closed. `agent_router.py`'s `/agent/start`+`/agent/answer` (the old fixed-questionnaire endpoints) now likely have zero real callers left — not confirmed/removed in this pass, worth checking in a follow-up cleanup.
- **Twilio not wired to a funded account.** OTP codes currently log to the server console in dev (`OtpService.java`'s fallback) rather than actually sending SMS. Fine for continued dev/testing; not fine for any real pilot user.
- ~~ai_service does not verify identity on every endpoint yet~~ — **re-audited 2026-07-04, this note was stale.** JWT verification (`jwt_auth.py`) is actually wired into every citizen-scoped endpoint: `/orchestrator/chat`, `/ws/session/{id}`, `/agents/financial-plan`, `/agents/grievance`, `/agents/trace/{session_id}` (ownership-checked), `/agents/csc/alternatives` (operator-role). `/agents/document/verify-ppo` and `/ocr/scan` genuinely have no citizen identity — both correct as-is: stateless, zero-retention, no per-citizen Mongo read/write, IP-rate-limited where relevant. **Real bug found and fixed in the same audit**: `/ocr/scan` (the Jan-Sahayak Lens scanner ChatPage calls directly from the browser) required `X-API-Key` — a header a browser page can never hold — so every real scan was silently 403ing in production. Same class of bug as the CSC endpoint fixed earlier the same day; dropped the API-key gate, verified a real scan now returns 200 through the vite proxy with zero API key. While in that file, also fixed a second, independent finding: it validated uploads via client-supplied `content_type`/filename extension only (spoofable), violating CLAUDE.md's non-negotiable OCR rule #2 ("use python-magic... do NOT trust the file extension") — added real byte-signature sniffing via `python-magic` (added to requirements.txt; needs system `libmagic1`, already present here). Verified: a `.txt` renamed `.jpg` is now rejected 415 with the true detected mime shown; a real PAN-card-style test image with visible text now scans end-to-end (200, correct doc_type, masked ID, seal keyword detected).
- ~~No CI pipeline~~ — **`.github/workflows/ci.yml` created 2026-07-04, not yet pushed** (creating a file is safe to do unprompted; pushing/committing is not, per this session's own working rules — needs an explicit ask). Jobs: frontend `npm ci && npm run build`, Python import-check + **the pytest suite** (added 2026-07-08, see below), and `mvn -B -q compile` for Spring Boot. All verified locally, incl. — critically — testing the Python steps against a **genuinely fresh venv**, not the long-lived dev one, which surfaced the `requirements.txt` bug below.
- ~~No real automated test suite~~ — **58-test pytest suite added 2026-07-08** (`ai_service/tests/`, wired into CI's python job). Fast (~2.3s) and fully self-contained — no Mongo/Sarvam/LLM/network, so it runs anywhere. Covers the security-critical and self-built logic: PII masking + injection guard (CLAUDE.md rules #7/#8), the PPO/Aadhaar Levenshtein mismatch + date/name normalizers (Agent 4 core), `state_match_variants` (the real 2026-07-03 code↔name bug), `profile_learner._validated` (the whitelist that stops the LLM writing garbage/hallucinated fields into a citizen profile), the `/translate` cache-key, JWT verification (throwaway RSA keypair + monkeypatched public key → valid accepted, tampered/expired/wrong-key/refresh-type rejected, operator role-gate), and an app-smoke layer (every expected HTTP + WS route mounted, security headers stamped, OpenAPI builds). This is genuine unit coverage of the non-negotiable paths; end-to-end integration against the live stack is still hand-verified per session (documented in COMPLETED.md). `pytest`/`pytest-asyncio` added to requirements.txt; `pyjwt` upgraded to `pyjwt[crypto]` so the RS256 `cryptography` backend is an explicit dep, not a fragile transitive one via google-auth.
- ~~No Spring Boot JUnit tests~~ — **added 2026-07-08.** 15 JUnit tests (`deploy/backend/spring-gateway/src/test/.../security/`): `FieldEncryptionServiceTest` (AES-256-GCM round-trip, random-IV means identical plaintext → different ciphertext, GCM tamper rejection, SHA-256 hash determinism + salt-sensitivity + never-reversible, wrong-key-length rejected — CLAUDE.md security rule #4) and `JwtUtilsTest` (RS256 issue/verify against an ephemeral keypair, subject/role/type claims, access-vs-refresh type gating, garbage-returns-null-never-throws, and a token signed by a *different* keypair rejected — the Java mirror of ai_service's `test_jwt_auth.py`, so both services are pinned to the same token contract). Both are context-free (no Spring context, no Mongo), run in ~2s via `mvn test`. CI's `spring-compile` job upgraded to `spring-test` (`mvn -B test` compiles AND runs them). Remaining: no controller/integration tests (those need a Mongo test container) — the unit layer covers the security-critical pure logic, which is the highest-value slice.
- ~~Legacy dead endpoints, not removed~~ — **`/chat/`, `/chat/stream`, `/voice/transcribe`, `/voice/query` removed 2026-07-04.** `chat.py` (old ChromaDB RAG chatbot) and `voice.py` (old local-Whisper transcribe/query) had zero frontend callers left and were confirmed self-contained — nothing else imports them (their shared dependency, `rag_chain.py`, is used independently by `main.py`'s `/health` endpoint and stays). Deleted both files + their `main.py` mounts + two now-unused rate limiters (`chat_limiter`, `voice_limiter`). `/status/check` (`status_tracker.py`) was deliberately left alone — also currently uncalled by the frontend, but flagged in Phase 6 as a scraper-based fallback path worth keeping for Agent 5. Verified: all four removed paths 404 on the real backend port; `/agent/start`, `/health`, `/`, `/openapi.json` (things that share startup code) still 200; both builds clean.
- ~~"Jan Sahayak" helper marketplace — undocumented, unauthenticated~~ — **deprecated and removed 2026-07-04, by explicit decision.** That sweep also turned up a whole separate pre-rebuild feature (`jan_sahayak.py`, 4 frontend pages) with no auth on any endpoint (IDOR risk on helper dashboard/login-by-id), permanent unencrypted storage of raw uploaded ID documents, a hardcoded fallback admin secret, a raw SQLite db instead of Mongo, and zero mention anywhere in CLAUDE.md. Confirmed zero live users/documents (`sahayak.db` had 0 helpers, 0 appointments; `secure_uploads/` was empty) before removal, so nothing was at risk. Given the choice (deprecate / harden / leave as-is), the call was to remove it — Agent 9 (CSC Assist) already covers the "human help" need properly (operator-mediated, Mongo-backed, JWT-gated). Removed: `ai_service/routers/jan_sahayak.py`, `ai_service/sahayak.db`, `ai_service/secure_uploads/`, `frontend/src/pages/Helper{Finder,Registration,Login,Dashboard}Page.{jsx,css}`, their routes/imports in `App.jsx`, the `/sahayak` vite proxy entry, and the router mount in `main.py`. Two dangling "Get Offline Help" CTAs (`SchemeDetailPage.jsx`, `ApplyMethodModal.jsx`) that linked to the deleted `/helpers` route now point at `/csc-finder` instead, with copy corrected to describe a real CSC visit rather than the removed "Jan Sahayak" human-helper promise.
- ~~conversation_sessions.summary not written~~ — **closed 2026-07-04.** WS disconnect is the session-end trigger (`graph/session_summary.py`, fire-and-forget from ws_router's finally block; `summarizedMessageCount` makes re-connects idempotent). REST-only sessions still get no summary — acceptable, the UI is WS-first.
