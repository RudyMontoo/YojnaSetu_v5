[pytest]
# Run from repo root so `import ai_service.*` resolves (ai_service is a package).
#   ai_service/venv/bin/python -m pytest
testpaths = ai_service/tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
